import TextLogo from "@repo/ui/textLogo";

const Header = () => {
  return (
    <header className="py-[1.4rem] flex justify-center items-center border-b">
      <TextLogo />
    </header>
  );
};

export default Header;
